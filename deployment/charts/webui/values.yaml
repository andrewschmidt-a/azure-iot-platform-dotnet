# Default values for webui.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: azureiot3m/webui
  tag: latest
  pullPolicy: Always

imagePullSecrets: []
nameOverride: "webui"
fullnameOverride: "webui"

probes:
  enabled: true

service:
  type: ClusterIP
  port: 10080

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
  #have to specify host port and then the backend service will be populated else we could just add the backend service and service port
    #- host: chart-example.local
    #  paths: []

  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local
secrets: {}
secretsRef: 
  - AppConfigurationConnectionString
resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
