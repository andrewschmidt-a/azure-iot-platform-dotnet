apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iothub-manager.fullname" . }}-test-connection"
  labels:
{{ include "iothub-manager.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "iothub-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
