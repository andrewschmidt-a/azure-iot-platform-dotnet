apiVersion: v1
kind: Service
metadata:
  name: {{ include "asa-manager.fullname" . }}
  labels:
{{ include "asa-manager.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "asa-manager.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
