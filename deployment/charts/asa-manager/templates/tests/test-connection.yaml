apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "asa-manager.fullname" . }}-test-connection"
  labels:
{{ include "asa-manager.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "asa-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
