apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "device-telemetry.fullname" . }}-test-connection"
  labels:
{{ include "device-telemetry.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "device-telemetry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
