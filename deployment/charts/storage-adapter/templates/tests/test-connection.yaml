apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-adapter.fullname" . }}-test-connection"
  labels:
{{ include "storage-adapter.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "storage-adapter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
