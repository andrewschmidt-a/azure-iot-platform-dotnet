apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identity-gateway.fullname" . }}-test-connection"
  labels:
{{ include "identity-gateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "identity-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
