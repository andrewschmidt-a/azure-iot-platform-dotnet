apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-adapter.fullname" . }}
  labels:
{{ include "storage-adapter.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "storage-adapter.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
