apiVersion: v1
kind: Service
metadata:
  name: {{ include "mmm-iot-service.fullname" . }}
  labels:
{{ include "mmm-iot-service.labels" . | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "mmm-iot-service.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
