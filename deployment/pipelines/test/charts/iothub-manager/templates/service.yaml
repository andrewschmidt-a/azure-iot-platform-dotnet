apiVersion: v1
kind: Service
metadata:
  name: {{ include "iothub-manager.fullname" . }}
  labels:
{{ include "iothub-manager.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "iothub-manager.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
