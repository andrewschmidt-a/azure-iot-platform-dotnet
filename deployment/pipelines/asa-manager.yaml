trigger:
  branches:
    include:
    - master 
  paths:
    include:
    - config/
    - common/

pr:
  branches:
    include:
    - master
  paths: 
    include:
    - config/
    - common/

stages:
- template: 'templates/service-deploy-env.yaml'
  parameters:
    service: asa-manager
 