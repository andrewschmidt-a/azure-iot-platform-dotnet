apiVersion: v1
kind: Service
metadata:
  name: {{ template "device-telemetry.fullname" . }}-svc
  labels:
    app: {{ template "device-telemetry.name" . }}
    chart: {{ template "device-telemetry.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "device-telemetry.name" . }}
    release: {{ .Release.Name }}
