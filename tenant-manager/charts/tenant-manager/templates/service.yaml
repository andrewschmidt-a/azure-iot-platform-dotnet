apiVersion: v1
kind: Service
metadata:
  name: {{ template "tenant-manager.fullname" . }}
  labels:
    app: {{ template "tenant-manager.name" . }}
    chart: {{ template "tenant-manager.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tenant-manager.name" . }}
    release: {{ .Release.Name }}
