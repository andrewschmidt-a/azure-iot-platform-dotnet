apiVersion: v1
kind: Service
metadata:
  name: {{ template "generic-service.fullname" . }}-svc
  labels:
    app: {{ template "generic-service.name" . }}
    chart: {{ template "generic-service.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "generic-service.name" . }}
    release: {{ .Release.Name }}
