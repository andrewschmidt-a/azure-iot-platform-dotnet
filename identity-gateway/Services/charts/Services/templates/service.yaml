apiVersion: v1
kind: Service
metadata:
  name: {{ template "tokengenerator.fullname" . }}
  labels:
    app: {{ template "tokengenerator.name" . }}
    chart: {{ template "tokengenerator.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tokengenerator.name" . }}
    release: {{ .Release.Name }}
