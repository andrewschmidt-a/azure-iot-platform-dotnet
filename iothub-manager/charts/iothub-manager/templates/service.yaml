apiVersion: v1
kind: Service
metadata:
  name: {{ template "iothub-manager.fullname" . }}-svc
  labels:
    app: {{ template "iothub-manager.name" . }}
    chart: {{ template "iothub-manager.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "iothub-manager.name" . }}
    release: {{ .Release.Name }}
